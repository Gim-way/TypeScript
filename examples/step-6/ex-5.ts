class UserS6EX5 {
  role?: 'admin' | 'user'

  constructor(private name: string) {
  }

  login(): void {
  }
}

function createUser(user: UserS6EX5) {
  // логика
  const defaultUser = new UserS6EX5('defauilt')
  defaultUser.role = undefined

  switch (user.role) {
  case 'admin':
    const a = 7
  case 'user':
    return true
    const c = 1
  }
}

interface IChecks {
  [check: string]: boolean;
}

const cS6EX5: IChecks = { kpp: true }
const dS6EX5 = cS6EX5['drive']
cS6EX5.drive

class VipUser extends UserS6EX5 {
  override login(): void {

  }
}